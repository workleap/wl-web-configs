---
"@workleap/rslib-configs": patch
---

Bumped the dependencies, which required to fix a few types that are not exported anymore from @rslib/core. In this process the peerDependency on @rsbuild/core has been removed.
